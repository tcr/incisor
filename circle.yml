machine:
  environment: {}
dependencies:
  override:
    - echo Setup Linux
test:
  override:
    - (pwd; cd /)
    - (pwd; cd /)
    - (pwd; cd /)
    - pwd; cd /
    - pwd; cd /
    - pwd; cd /
general:
  build_dir: .
